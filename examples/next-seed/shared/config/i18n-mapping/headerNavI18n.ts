import { COMMON_ADMIN_NAV_DASHBOARD } from '../i18n-identifier/common/common';

/**
 * App header nav i18n interface (Admin link; home via logo).
 */
export type HeaderNavI18nInterface = typeof headerNavI18n;

export const headerNavI18n = Object.freeze({
  navAdmin: COMMON_ADMIN_NAV_DASHBOARD
});
