---
description:
globs: **/*.ts,**/*.tsx
alwaysApply: false
---

# TypeScript 面向接口/对象开发协作规范

1. **语言约定**：全程中文（代码注释、提交信息、技术讨论）。
2. **方案先行**：先输出技术方案（含接口定义与类结构图），用户确认“确认”后方可编码。
3. **疑问暂停**：需求模糊时立即停止并列出疑问，严禁猜测实现。
4. **增量交付**：复杂功能拆分为可独立运行的小步骤，逐步交付验证。

## 二、架构与设计原则

5. **契约优先**：业务代码必须先定义接口/抽象类；纯工具类（无状态/纯函数）允许直接实现。
6. **依赖倒置**：高层模块仅依赖抽象接口，具体实现在入口层组装。
7. **单一职责**：每个类/接口仅负责一个业务领域，超过 500 行(不包含注释)或功能混杂必须拆分。
8. **开闭原则**：新增功能只增新类/接口，严禁修改已稳定运行的旧逻辑（除修 Bug 外）。

## 三、命名与特殊语法规范

9. **命名核心法则**：
   - **文件/类/接口**：大驼峰 (PascalCase)，禁 `I` 前缀，强制语义后缀（`Interface`, `Service`, `Engine`, `Store`, `Util`, `Error`）。
   - **类型别名 (Type)**：必须使用大驼峰并以 **`Type` 结尾**（如 `UserProfileType`, `ApiResponseType`）。
   - **枚举替代**：**严禁使用 `enum`**，统一使用 **`const` 对象 + `as const`** 断言定义常量集合（如 `const OrderStatus = { ... } as const`）。
   - **方法/变量**：小驼峰 (camelCase)，方法名动词开头，禁止不明简写。
   - **常量**：全大写下划线 (UPPER_SNAKE_CASE)。

## 四、TypeScript 编码硬规

10. **显式类型**：所有参数、返回值、属性必须显式声明类型，严禁隐式推断或滥用 `any`。
11. **不可变数据**：接口属性默认添加 `readonly`，确需修改时单独定义可变接口。
12. **访问控制**：接口方法默认 `public`；类内部优先 `protected` 以支持扩展，仅核心敏感逻辑用 `private`。
13. **异步规范**：所有 I/O 操作必须返回 `Promise` 并使用 `async/await`。

## 五、文件与组件职责边界

18. **文件单一导出**：一个文件要么只导出工具函数集合，要么只导出一个类/组件，严禁混合。工具函数文件以 `Util` 结尾，类/组件文件以其语义名称命名。
19. **组件单一职责**：UI 组件只做一件事。展示逻辑（渲染）与交互编排（状态切换、条件分支）必须分层：
    - **叶子组件**：只负责单一 UI 片段（如纯输入区、纯预览区），不持有与自身渲染无关的状态。
    - **编排组件**：负责将叶子组件组合、控制显隐/切换、向下传递事件，文件名可加 `Panel` / `Container` 后缀以示区分。
20. **表单与展示分离**：表单组件只捕获用户输入，不承担数据展示（如 Markdown 预览）；展示组件只渲染数据，不持有提交逻辑。两者均由上层编排组件按需组合。
21. **样式类名管理**：组件 `className` 必须使用 `clsx` 管理，严禁用模板字符串拼接（如 `` `${base} ${condition ? 'a' : ''}` ``），条件类名和固定类名一律通过 `clsx(...)` 组合。
22. **组件 TSX 禁止 JSX 块注释**：不得在 JSX/TSX 组件内写 `{/* ... */}`（无论中英文）；禁止用注释充当区块标题、原型对齐说明或布局旁白。意图通过子组件命名、文件顶部 JSDoc 注释、注释块等表达。

### 小功能目录组合（可选）

将**一个独立小特性**收敛在同一目录时使用；此处为**特性内局部**组织，与全局 `shared/config` 等不冲突。

| 路径 / 文件 | 职责 |
| --- | --- |
| **`config/`** 或根下 `*Config.ts`、常量模块 | 本特性专用**配置项**与**常量**（`const` + `as const`）；不写业务流程分支。 |
| **`XxxUtil.ts`（优先）** 或 **`utils/`**（按需） | **无状态工具**：默认根下**单文件或少量 `Util` 文件**；**仅当**工具代码过多、需拆多文件时再建 **`utils/`**；文件名以 `Util` 结尾，遵守第 18 条单一导出约定。 |
| **`index.ts`** | **对外唯一入口**：集中 `export`；外部优先从该目录根导入，避免深层路径耦合。 |
| **其余具名文件（`xxx`）** | **主实现**：`Service`、`Panel`、`Engine`、`Store`、Hook 等，一文件一主责；命名遵循第 9 条后缀法则。 |

**原则**：`config` / 工具层不放 UI 与编排；工具**优先单文件 `Util`**，**避免过早建 `utils/` 目录**；主实现不直接散落魔法值；对外只暴露 `index.ts` 中声明的符号。

23. **组件属性**: 组件内渲染的第一个根节点需要加上 data-testid 属性，用于测试。

## 六、质量与测试要求

24. **注释即文档**：公共接口/类必须包含 JSDoc（用途、参数、返回值），必要时加 `@link` 跳转。
25. **测试驱动**：核心逻辑必须伴随单元测试，演示如何通过 Mock 接口隔离测试。
26. **错误冒泡**：下层只抛出不捕获，仅在上层边界统一处理异常（资源清理除外）。
27. **异常标准化**：优先自定义类继承 `Error`（携带业务码），严禁抛出字符串，无分类时可用原生 `Error`。
